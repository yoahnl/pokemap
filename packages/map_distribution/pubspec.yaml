name: map_distribution
description: Pure Dart distribution contracts and tooling for PokeMap games.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  archive: 4.0.9
  crypto: ^3.0.7
  map_core:
    path: ../map_core
  path: ^1.9.1
  pub_semver: ^2.2.0
  unorm_dart: ^0.3.2

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
