enum EditorToolType {
  selection,
  tilePaint,
  collisionPaint,
  entityPlacement,
  warpPlacement,
  triggerPlacement,
  eraser,
}

enum LayerType {
  tile,
  collision,
  entity,
  warp,
  trigger,
}

enum EntityType {
  npc,
  monster,
  chest,
  sign,
  custom,
}

enum TriggerType {
  script,
  sound,
  cutscene,
  battle,
  custom,
}

enum ProjectVersion {
  v1,
}
