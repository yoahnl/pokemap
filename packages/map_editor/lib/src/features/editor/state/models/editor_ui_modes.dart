/// Comportement du pinceau collision dans l'éditeur.
enum CollisionBrushSizeMode {
  brushFootprint,
  singleTile,
}

/// Mode courant de la colonne "tiles / elements" dans l'éditeur.
enum TilesElementsPanelMode {
  palette,
  placedInstances,
}
