name: map_authoring
description: >-
  Canonical pure-Dart authoring contracts and orchestration for PokeMap.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  map_core:
    path: ../map_core

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
